#include "MeshComponent.h"

MeshComponent::MeshComponent(class GameObject* owningGameObject, 
	PATH meshPath, PATH materialPath,
	PATH vertexShaderPath, PATH pixelShaderPath, MeshType meshType) : ComponentBase(owningGameObject)
{

}
